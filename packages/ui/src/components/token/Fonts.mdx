import { Meta, Typeset } from "@storybook/blocks";
import resolveConfig from "tailwindcss/resolveConfig";
import tailwindConfig from "../../../tailwind.config.js";

export const { theme } = resolveConfig(tailwindConfig);
export const fontSizes = Object.values(theme.fontSize).map((item) =>
  Array.isArray(item) ? item[0] : item,
);

<Meta title="Tokens/Fonts" />

# Fonts

## IBM Plex Mono

<Typeset fontSizes={fontSizes} fontFamily={theme.fontFamily.mono} />

## Public Sans

<Typeset fontSizes={fontSizes} fontFamily={theme.fontFamily.sans} />
